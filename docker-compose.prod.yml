services:
  was:
    image: malhaebom/backend:latest
    restart: unless-stopped
    ports:
      - "80:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
    volumes:
      - ./config:/app/config:ro
